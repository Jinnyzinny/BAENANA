export type Category = {
  id: number;
  name: string;
  description: string;
};

export type HealthInfo = {
  id: number;
  title: string;
  content: string;
  imageUrl: string;
  createdAt: string;
};
